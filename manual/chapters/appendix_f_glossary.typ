#import "../template.typ": *
#set-appendix(6, "Glossary")

= Glossary

/ AMSDOS: The CPC's disc operating system, layered over BASIC, providing disc
  file commands.

/ ASIC: The custom chip in the Plus machines that replaces the discrete Gate
  Array and CRTC and adds sprites, an enhanced palette, and DMA sound.

/ CRTC: Cathode Ray Tube Controller --- the chip that generates video timing.
  The CPC shipped with several types, all emulated.

/ Gate Array: Amstrad's custom video and memory-management chip.

/ IPC: The TCP control interface konCePCja exposes on port 6543 for scripting
  and debugging (Chapter 9).

/ Mickey: One unit of mouse movement, as reported by the AMX Mouse.

/ PSG: Programmable Sound Generator (the AY-3-8912) --- the CPC's sound chip.

/ Snapshot: A saved copy of the entire machine state, in a `.sna` file, that
  resumes instantly.

/ M4 Board: A network and storage expansion, emulated with a web file manager
  (Chapter 11).
